import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Worker = () => {
  // Worker name, phone no,
  return (
    <View>
      <Text>Worker</Text>
    </View>
  );
};

export default Worker;

const styles = StyleSheet.create({});
